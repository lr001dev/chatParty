<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Update My Profile</h1>
    <form class="" action="/members/<%=currentUser._id%>?_method=PUT" method="POST">
      <input type="text" name="userName" placeholder="username" value="<%= currentUser.userName %>">
      <input type="hidden" name="password" placeholder="password" value="<%= currentUser.password %>">
      <input type="text" name="firstName" placeholder="first name" value="<%= currentUser.firstName %>">
      <input type="text" name="img" placeholder="photo url" value="<%= currentUser.img %>">
      <input type="text" name="bio" placeholder="bio" value="<%= currentUser.bio %>">
      <input type="submit" name="" value="SUBMIT">
    </form>
  </body>
</html>
